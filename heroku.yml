setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    backend:
      dockerfile: packages/backend/Dockerfile
    root:
      dockerfile: Dockerfile
    client:
      dockerfile: packages/backend/Dockerfile