setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    backend:
      dockerfile: packages/backend/Dockerfile
      context: .
    client:
      dockerfile: packages/backend/Dockerfile
      context: .
